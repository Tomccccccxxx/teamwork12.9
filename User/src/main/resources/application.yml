spring:
  application:
    name: user-service # 服务名称
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 # Nacos 服务器地址
      config:
        server-addr: localhost:8848 # Nacos 服务器地址
        file-extension: yaml # 如果你使用 YAML 格式的配置文件
        group: DEFAULT_GROUP # 可选，默认组名
        namespace: # 可选，默认命名空间